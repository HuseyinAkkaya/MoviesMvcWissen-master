
@model _036_MoviesMvcWissen.Models.DirectorIndexViewModel

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           @* @Html.DisplayNameFor(model => model.Directors.First().Name)*@
            @Html.Label("lName","Director Name")
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Directors.FirstOrDefault().Surname)*@
            @Html.Label("lSurname", "Director Surname")
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Directors.FirstOrDefault().Retired)*@
            @Html.Label("lRetired", "Director Retired")
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.Directors)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Retired)
            </td>
            <td>
                @Html.ActionLink(" ", "Edit", new { id = item.Id }, new { @class = "fa fa-edit btn btn-danger" }) |
                @Html.ActionLink(" ", "Delete", new { id = item.Id }, new { @class = "fa fa-trash-o btn btn-danger" }) |
                @Html.ActionLink(" ", "Details", new { id = item.Id }, new { @class = "fa fa-info btn btn-warning" })
            </td>
        </tr>
    }

</table>
